#include "admin.h"





void Admin::add_data()
{
    ofstream out("sdm_dat.csv", ios_base::app);
    if (out.is_open())
    {
        Student student;
                vector<string> temp;
                cin.ignore();
                adds(5,5);
    }
}
